export interface UserInterface {
  _id: string;
  name: string;
  prevPrice: number;
  currPrice: number;
  avatar: string;
}

export interface OrganicInterface {
  _id: string;
  name: string;
  prevPrice: number;
  currPrice: number;
  avatar: string;
  category:String
}
